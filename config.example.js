module.exports = {
    clientEmail: '', // Google account email (OR set with env GOOGLE_CLIENT_EMAIL)
    privateKey: '', // Google API key (OR set with env GOOGLE_PRIVATE_KEY)
    sheetId: '', // Google sheet id to use (OR set with env GOOGLE_SHEET_ID)
};
